const upperCase = string => string.toUpperCase();

const firstLetter = string => string[0];

const sumStrings = (stringOne, stringTwo) => stringOne + stringTwo;

module.exports = { 
  upperCase,
  firstLetter,
  sumStrings,
}
